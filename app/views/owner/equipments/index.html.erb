<div class="my-equipments-title d-flex align-middle justify-content-between" style="margin-left:160px;">
  <h2 class="pr-3 mt-4">Mes annonces</h2>
  <%= link_to new_owner_equipment_path do %>
    <p class="btn btn-flat" style="margin-right:155px;" >+</p>
  <% end %>
</div>


<div class="container">
  <div class="row">
    <% @equipments.each do |equipment| %>
    <div class="col-6 mb-3 p-2">
      <div class="my-equipments-card d-flex">
        <div class="flex-max-40">
          <%= cl_image_tag(equipment.picture,width: 150, height: 190, crop: :fill, class:'mx-5 mt-4') %>
        </div>
        <div class="flex-max-50 d-flex flex-column justify-content-between">
          <div class="">
            <h5 class="mt-3"><%=equipment.title%></h5>
            <p class="text-my-equip mb-2"><%=equipment.material_category%></p>
            <p class=""><%= raw equipment.details.gsub(".", ".<br>") %></p><br>
          </div>
          <div class="d-flex justify-content-between">
            <div class="d-flex mb-3">
              <%= image_tag 'location.svg', height: 25, width: 25, crop: :fill, class:'mb-2' %>
              <p class="pl-2 mt-1" style="font-size:14px;"><%= equipment.location.match(/\s(\w+)$/) %></p>
            </div>
            <p class='card-index-price mr-3' style="font-size:20px; "><%= equipment.price_per_day %> € / jour</p>
          </div>
        </div>
      </div>
    </div>
      <% end %>
    </div>
  </div>
</div>

